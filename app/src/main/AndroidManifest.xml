<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wlm.bookkeeping">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/icon_logon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_logon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bookkeeping">
        <activity android:name=".ui.BillActivity"/>
        <activity android:name=".ui.GatherActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>